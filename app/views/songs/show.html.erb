<h1> Song details </h1>

<h1>Song name: <%= @song.name %></h1>
<li>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) %></li>
<li>Genre: <%= link_to @song.genre.name, genre_path(@song.genre) %></li>
<br><br><br>


<%= link_to 'Back', songs_path %><br>
<%= link_to 'Edit song', edit_song_path(@song) %><br>
<%= link_to 'Delete song', song_path, method: :delete, data: { confirm: "Are you sure?" } %>
